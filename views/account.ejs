<!-- views/account.ejs -->
<!doctype html>
<html>
<head>
    <title>Node Authentication</title>
    <!-- Compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/css/materialize.min.css">

    <!-- Compiled and minified JavaScript -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/js/materialize.min.js"></script>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
 
    <script src="https://code.jquery.com/jquery.js"></script>
  <link rel="stylesheet" type="text/css" href="/assets/css/style.css">
  </head>
 
  <body class="grey">
    <div class="container main">
      <div class="row">
    <div class="col s6 offset-s3 borderclass grey darken-1 z-depth-5">
           <h1 class="center-align thin white-text"><span class="fa fa-sign-in"></span>Create Listing</h1>
          <ul class="list-group todo-list">
            <li class="list-group-item new-item">
              <form id="user-form">
              <input class="new-item" placeholder="add a name" type="text" />
              <input class="new-job" placeholder="add a job" type="text" />
              <a type="submit" href="/profile" class="btn btn-default center-align grey darken-3 thin fa fa-user">Submit</a>
              </form>
            </li>
          </ul>
        </div>
      </div>
    </div>
    <footer class="page-footer">
    <div class="center-align">
        ©2018 RTU-31
    </div>
</footer>
    <!-- Custom Script -->
    <script href='/public/js/view/js' type="text/javascript">
    $(document).ready(function() {
      // Getting a reference to the input field where user adds a new user
      var $newItemInput = $("input.new-item");
      var $newJobInput = $("input.new-job");
      // Adding event listeners 
      $(document).on("submit", "#user-form", insertTodo);
      // This function inserts a new user into our database and then updates the view
      function insertTodo(event) {
        event.preventDefault();
        console.log("it worked");
        var user = {
          Username: $newItemInput.val().trim(),
          Jobs: $newJobInput.val().trim(),
          About: null
        };
    
        $.post("/account", user);
        $newItemInput.val("");
      }
    });
    
    </script>
  
  </body>
  
  </html>